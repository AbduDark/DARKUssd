<Window x:Class="ModemPoolManager.TransferLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="سجل التحويلات" Height="500" Width="700"
        WindowStartupLocation="CenterOwner"
        Background="#0D1117"
        FlowDirection="RightToLeft">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="📋" FontSize="24" Margin="0,0,10,0"/>
            <TextBlock Text="سجل التحويلات" FontSize="22" FontWeight="Bold" Foreground="#FF7900" VerticalAlignment="Center"/>
        </StackPanel>
        
        <Border Grid.Row="1" Background="#000000" CornerRadius="8" Padding="16" BorderBrush="#30363D" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <TextBlock x:Name="LogTextBlock" FontSize="13" 
                           Foreground="#3FB950" FontFamily="Consolas" TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,16,0,0">
            <Button Content="📋 نسخ السجل" Click="CopyLog_Click" 
                    Background="#1F6FEB" Foreground="White" Padding="16,10" 
                    BorderThickness="0" Cursor="Hand" FontSize="14" FontWeight="SemiBold">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="❌ إغلاق" Click="Close_Click" Margin="8,0,0,0"
                    Background="#DA3633" Foreground="White" Padding="16,10" 
                    BorderThickness="0" Cursor="Hand" FontSize="14" FontWeight="SemiBold">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</Window>

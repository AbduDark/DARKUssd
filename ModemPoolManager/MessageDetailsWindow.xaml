<Window x:Class="ModemPoolManager.MessageDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:ModemPoolManager.Converters"
        Title="تفاصيل الرسالة" 
        Height="500" Width="450"
        WindowStartupLocation="CenterOwner"
        FlowDirection="RightToLeft"
        Background="#0D1117"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <converters:OperatorToBrushConverter x:Key="OperatorToBrushConverter"/>
        
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#21262D"/>
            <Setter Property="Foreground" Value="#C9D1D9"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#30363D"/>
                    <Setter Property="BorderBrush" Value="#8B949E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CopyButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#1F6FEB"/>
            <Setter Property="BorderBrush" Value="#1F6FEB"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#388BFD"/>
                    <Setter Property="BorderBrush" Value="#388BFD"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DeleteButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#DA3633"/>
            <Setter Property="BorderBrush" Value="#DA3633"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F85149"/>
                    <Setter Property="BorderBrush" Value="#F85149"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ReplyButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#238636"/>
            <Setter Property="BorderBrush" Value="#238636"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2EA043"/>
                    <Setter Property="BorderBrush" Value="#2EA043"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="#8B949E"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="16"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#21262D"/>
                    <Setter Property="Foreground" Value="#F0F6FC"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border CornerRadius="12" BorderThickness="2" Background="#0D1117"
            BorderBrush="{Binding OperatorName, Converter={StaticResource OperatorToBrushConverter}}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#161B22" Padding="16,12" CornerRadius="10,10,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="📩" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="تفاصيل الرسالة" FontSize="18" FontWeight="Bold" Foreground="#F0F6FC" VerticalAlignment="Center"/>
                    </StackPanel>

                    <Button Grid.Column="1" Content="✕" Style="{StaticResource CloseButton}" 
                            Click="CloseButton_Click" ToolTip="إغلاق"/>
                </Grid>
            </Border>

            <Border Grid.Row="1" Background="#161B22" Margin="16,16,16,0" Padding="16" CornerRadius="8"
                    BorderBrush="{Binding OperatorName, Converter={StaticResource OperatorToBrushConverter}}" BorderThickness="0,0,0,3">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="المرسل:" Foreground="#8B949E" FontSize="13" Margin="0,0,10,8"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" x:Name="SenderNumber" FontSize="18" FontWeight="Bold" 
                               Foreground="{Binding OperatorName, Converter={StaticResource OperatorToBrushConverter}}" 
                               FlowDirection="LeftToRight" Margin="0,0,0,8"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="التاريخ:" Foreground="#8B949E" FontSize="13" Margin="0,0,10,8"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" x:Name="MessageDate" FontSize="14" Foreground="#C9D1D9" Margin="0,0,0,8"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="المودم:" Foreground="#8B949E" FontSize="13" Margin="0,0,10,0"/>
                    <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
                        <TextBlock x:Name="ModemInfo" FontSize="14" Foreground="#58A6FF"/>
                        <Border x:Name="StatusBadge" CornerRadius="4" Padding="6,2" Margin="10,0,0,0" VerticalAlignment="Center">
                            <TextBlock x:Name="StatusText" FontSize="11" FontWeight="SemiBold"/>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Row="2" Background="#161B22" Margin="16,16,16,0" Padding="16" CornerRadius="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="📝" FontSize="14" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="نص الرسالة" Foreground="#8B949E" FontSize="13" VerticalAlignment="Center"/>
                    </StackPanel>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="180">
                        <TextBlock x:Name="MessageContent" TextWrapping="Wrap" FontSize="15" Foreground="#E6EDF3" 
                                   LineHeight="24" Padding="4"/>
                    </ScrollViewer>
                </Grid>
            </Border>

            <Border Grid.Row="3" x:Name="OtpSection" Background="#0D419D" Margin="16,16,16,0" Padding="12" CornerRadius="8" Visibility="Collapsed">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="🔑" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="كود التحقق:" Foreground="#A5D6FF" FontSize="13" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock x:Name="OtpCode" FontSize="22" FontWeight="Bold" Foreground="#58A6FF" 
                                   FlowDirection="LeftToRight" VerticalAlignment="Center"/>
                    </StackPanel>

                    <Button Grid.Column="1" Content="📋 نسخ الكود" Style="{StaticResource CopyButton}" 
                            Padding="12,6" Click="CopyOtp_Click" ToolTip="نسخ كود التحقق"/>
                </Grid>
            </Border>

            <Border Grid.Row="4" Background="#161B22" Margin="0,16,0,0" Padding="16" CornerRadius="0,0,10,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Content="📋 نسخ" Style="{StaticResource CopyButton}" 
                            Click="CopyMessage_Click" ToolTip="نسخ نص الرسالة"/>
                    <Button Grid.Column="1" Content="↩️ رد" Style="{StaticResource ReplyButton}" 
                            Click="Reply_Click" ToolTip="الرد على الرسالة"/>
                    <Button Grid.Column="2" Content="🗑️ حذف" Style="{StaticResource DeleteButton}" 
                            Click="Delete_Click" ToolTip="حذف الرسالة"/>
                    <Button Grid.Column="3" Content="إغلاق" Style="{StaticResource ActionButton}" 
                            Click="CloseButton_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>

<Window x:Class="ModemPoolManager.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="الإعدادات - Settings" 
        Height="550" Width="500"
        FlowDirection="RightToLeft"
        Background="#0D1117"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#238636"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2EA043"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="GhostButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#8B949E"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#21262D"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#0D1117"/>
            <Setter Property="Foreground" Value="#C9D1D9"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="CaretBrush" Value="#58A6FF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#C9D1D9"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style x:Key="CardPanel" TargetType="Border">
            <Setter Property="Background" Value="#161B22"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
        </Style>
    </Window.Resources>

    <Border Background="#0D1117" BorderBrush="#30363D" BorderThickness="1" CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#161B22" CornerRadius="12,12,0,0" Padding="16,12" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⚙" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock Text="الإعدادات" FontSize="16" FontWeight="Bold" Foreground="#F0F6FC" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button Grid.Column="1" Content="✕" Click="CloseButton_Click" 
                            Background="Transparent" Foreground="#8B949E" BorderThickness="0"
                            FontSize="16" Padding="8,4" Cursor="Hand"/>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="16">
                <StackPanel>
                    <Border Style="{StaticResource CardPanel}">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                <TextBlock Text="🔧" FontSize="18" Margin="0,0,8,0"/>
                                <TextBlock Text="إعدادات المودم" FontSize="16" FontWeight="Bold" Foreground="#F0F6FC"/>
                            </StackPanel>

                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Baud Rate:" Foreground="#8B949E" VerticalAlignment="Center"/>
                                <TextBox Grid.Column="1" x:Name="BaudRateTextBox" Style="{StaticResource ModernTextBox}"/>
                            </Grid>

                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Read Timeout:" Foreground="#8B949E" VerticalAlignment="Center"/>
                                <TextBox Grid.Column="1" x:Name="ReadTimeoutTextBox" Style="{StaticResource ModernTextBox}"/>
                            </Grid>

                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Max Modems:" Foreground="#8B949E" VerticalAlignment="Center"/>
                                <TextBox Grid.Column="1" x:Name="MaxModemsTextBox" Style="{StaticResource ModernTextBox}"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardPanel}" Margin="0,16,0,0">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                <TextBlock Text="🔐" FontSize="18" Margin="0,0,8,0"/>
                                <TextBlock Text="كلمات سر الكاش" FontSize="16" FontWeight="Bold" Foreground="#F0F6FC"/>
                            </StackPanel>

                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Background="#FF7900" CornerRadius="4" Padding="8,4" VerticalAlignment="Center" HorizontalAlignment="Left">
                                    <TextBlock Text="Orange" Foreground="White" FontWeight="Bold" FontSize="12"/>
                                </Border>
                                <TextBox Grid.Column="1" x:Name="OrangeCashPasswordTextBox" Style="{StaticResource ModernTextBox}" 
                                         FlowDirection="LeftToRight" FontFamily="Consolas"/>
                            </Grid>

                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Background="#E60000" CornerRadius="4" Padding="8,4" VerticalAlignment="Center" HorizontalAlignment="Left">
                                    <TextBlock Text="Vodafone" Foreground="White" FontWeight="Bold" FontSize="12"/>
                                </Border>
                                <TextBox Grid.Column="1" x:Name="VodafoneCashPasswordTextBox" Style="{StaticResource ModernTextBox}" 
                                         FlowDirection="LeftToRight" FontFamily="Consolas"/>
                            </Grid>

                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Background="#80B826" CornerRadius="4" Padding="8,4" VerticalAlignment="Center" HorizontalAlignment="Left">
                                    <TextBlock Text="Etisalat" Foreground="White" FontWeight="Bold" FontSize="12"/>
                                </Border>
                                <TextBox Grid.Column="1" x:Name="EtisalatCashPasswordTextBox" Style="{StaticResource ModernTextBox}" 
                                         FlowDirection="LeftToRight" FontFamily="Consolas"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardPanel}" Margin="0,16,0,0">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                <TextBlock Text="📅" FontSize="18" Margin="0,0,8,0"/>
                                <TextBlock Text="إعدادات الصلاحية" FontSize="16" FontWeight="Bold" Foreground="#F0F6FC"/>
                            </StackPanel>

                            <CheckBox x:Name="AutoRenewValidityCheckBox" 
                                      Content="تجديد صلاحية الخطوط تلقائياً عند التوصيل" 
                                      Foreground="#F0F6FC" FontSize="14" Margin="0,0,0,8"
                                      Style="{StaticResource ModernCheckBox}"/>
                            <TextBlock Text="عند تفعيل هذا الخيار، سيتم إرسال رسالة SMS إلى 2656 تلقائياً عند توصيل أي مودم لتجديد صلاحية الخط." 
                                       Foreground="#8B949E" FontSize="12" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Row="2" Background="#161B22" CornerRadius="0,0,12,12" Padding="16,12">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="💾 حفظ" Click="SaveButton_Click" Style="{StaticResource ModernButton}" Width="120"/>
                    <Button Content="إلغاء" Click="CancelButton_Click" Style="{StaticResource GhostButton}" Width="120"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
